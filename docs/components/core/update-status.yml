components:
  schemas:
    UpdateStatusSchema:
      type: object
      properties:
        status:
          type: string
          required: true
          enum: ['ativo', 'inativo']
          example: ativo
